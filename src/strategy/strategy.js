// period, onBuySignal, onSellSignal, activeTrades
const strategy = (onBuySignal, onSellSignal) => {
  const trades = [];
};

const positionOpened = (price, time, amount, order) => {};

const positionClosed = (price, time, amount, order) => {};

module.exports = strategy;
